import { Router } from "express";
import ScoreCard from "./models/ScoreCard.js";
  const router = Router();
  router.delete("/cards", ...);
  router.post("/card", ...);
  router.get("/cards", ...);
  export default router;
 